import { Link } from '@inertiajs/react';

export default function NavAuthButtons() {
    return (
        <div className="nav-auth">
            {/* <Link href={route('login')} className="nav-auth__signin">
                Sign in
            </Link>
            <Link href={route('login')} className="nav-auth__login">
                Login
            </Link> */}
        </div>
    );
}
